---
- name: Ensure linux drivers are installed
  become: true
  community.general.pacman:
    name: "{{ item }}"
    state: present
  loop:
    - vulkan-intel
    - libva-intel-driver
    - intel-media-driver
  tags: drivers
