- name: Setup Xorg environment
  become: true
  block:
    - name: Install required Xorg packages (Arch)
      community.general.pacman:
        name:
          - maim
          - picom
          - xclip
          - xcolor
          - xdotool
          - xorg-xev
          - screenkey
          - xorg-xinit
          - xorg-server
          - xorg-xrandr
          - xorg-xsetroot
        state: present

    - name: Remove Wayland-related packages
      community.general.pacman:
        name: waybar
        state: absent

    - name: Deploy vconsole configuration
      ansible.builtin.copy:
        src: "files/artix/etc/vconsole.conf"
        dest: "/etc/vconsole.conf"
        owner: root
        group: root
        mode: "0644"
        force: true
