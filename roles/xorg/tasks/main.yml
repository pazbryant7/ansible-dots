- name: Load OS-specific tasks if available
  ansible.builtin.stat:
    path: "{{ role_path }}/tasks/{{ host_os }}.yml"
  register: distribution_config

- name: Include OS-specific tasks
  ansible.builtin.include_tasks: "{{ host_os }}.yml"
  when: distribution_config.stat.exists

- name: Clone and build ST terminal emulator
  vars:
    personal_github: "git@personal.github:pazbryant7"
  block:
    - name: Clone ST repository
      ansible.builtin.git:
        repo: "{{ personal_github }}/st.git"
        dest: "{{ repos_path }}/st"
        version: main
        update: false

    - name: Build and install ST
      become: true
      ansible.builtin.command:
        cmd: make clean install
        chdir: "{{ repos_path }}/st"
        creates: /usr/local/bin/st

    - name: Symlink ST to xterm
      become: true
      ansible.builtin.file:
        src: /usr/local/bin/st
        dest: /usr/bin/xterm
        state: link

- name: Clone and build DWM window manager
  vars:
    personal_github: "git@personal.github:pazbryant7"
  block:
    - name: Clone DWM repository
      ansible.builtin.git:
        repo: "{{ personal_github }}/dwm.git"
        dest: "{{ repos_path }}/dwm"
        version: main
        update: false

    - name: Build and install DWM
      become: true
      ansible.builtin.command:
        cmd: make clean install
        chdir: "{{ repos_path }}/dwm"
        creates: /usr/local/bin/dwm

- name: Clone and build slstatus
  vars:
    personal_github: "git@personal.github:pazbryant7"
  block:
    - name: Clone slstatus repository
      ansible.builtin.git:
        repo: "{{ personal_github }}/slstatus.git"
        dest: "{{ repos_path }}/slstatus"
        version: main
        update: false

    - name: Build and install slstatus
      become: true
      ansible.builtin.command:
        cmd: make clean install
        chdir: "{{ repos_path }}/slstatus"
        creates: /usr/local/bin/slstatus

- name: Setup custom system configurations
  vars:
    files_base: files/common
    fonts_dest: "{{ ansible_user_dir }}/.local/share/fonts"
  block:
    - name: Setup personal fonts
      become: false
      block:
        - name: Ensure user font directory exists
          ansible.builtin.file:
            path: "{{ fonts_dest }}/bryant-fonts"
            state: directory
            mode: "0755"

        - name: Deploy personal fonts
          ansible.builtin.copy:
            src: "{{ files_base }}/.local/share/fonts/"
            dest: "{{ fonts_dest }}"
            mode: "0644"
          register: font_deployment

        - name: Refresh user font cache
          ansible.builtin.command: fc-cache -f -v
          when: font_deployment.changed

    - name: Setup system-wide configurations
      become: true
      block:
        - name: Ensure system directories exist
          ansible.builtin.file:
            path: "{{ item }}"
            state: directory
            mode: "0755"
          loop:
            - /usr/share/X11/xkb/symbols
            - /etc/X11/xorg.conf.d
            - /usr/share/kbd/keymaps/i386/dvorak

        - name: Deploy system configuration files
          ansible.builtin.copy:
            src: "{{ files_base }}{{ item.path }}"
            dest: "{{ item.path }}"
            mode: "0644"
            force: "{{ item.force | default(true) }}"
          loop:
            - { path: "/etc/environment" }
            - { path: "/etc/X11/xorg.conf.d/00-keyboard.conf" }
            - { path: "/usr/share/X11/xkb/symbols/", force: false }
            - { path: "/usr/share/kbd/keymaps/i386/dvorak/", force: false }
