#!/bin/sh

die() {
  printf 'Error: %s\n' "$1" >&2
  exit 1
}

check_root() {
  [ "$(id -u)" -ne 0 ] && die "This script must be run as root. Try: sudo $0"
}

flush_dns_cache() {
  resolvectl flush-caches
}

main() {
  check_root
  flush_dns_cache
}

main "$@"
