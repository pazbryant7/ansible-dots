---
- name: "Ensure base development packages are installed"
  become: true
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - ripgrep
    - openssh-client
    - fonts-noto-color-emoji
