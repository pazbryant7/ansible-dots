FROM archlinux:base-devel

RUN pacman -Sy --noconfirm

RUN useradd -m -G wheel -s /bin/sh bryant && \
  echo "%wheel ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

USER bryant
WORKDIR /home/bryant

CMD ["/bin/sh"]
